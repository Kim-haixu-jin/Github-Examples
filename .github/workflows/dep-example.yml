name: "Dep Jobs Example"

on: push

jobs:
  sayHi:
    runs-on: ubuntu-latest
    needs: sayThere
    steps:
      - name: stepA
        run: echo 'Hi'
  sayThere:
    runs-on: ubuntu-latest
    steps:
      - name: stepB
        run: echo 'there'
